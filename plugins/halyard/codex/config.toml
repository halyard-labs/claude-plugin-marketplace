# Halyard MCP server configuration for Codex CLI
# Copy this to ~/.codex/config.toml (global) or .codex/config.toml (project-scoped)

[mcp_servers.ask-expert]
type = "http"
url = "https://mcp.usehalyard.ai"
